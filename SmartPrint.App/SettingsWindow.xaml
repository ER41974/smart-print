<Window x:Class="SmartPrint.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resx="clr-namespace:SmartPrint.Core.Resources;assembly=SmartPrint.Core"
        Title="{x:Static resx:Strings.Settings}" Height="400" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <StackPanel Margin="20">
        <!-- Language -->
        <TextBlock Text="{x:Static resx:Strings.Language}"/>
        <ComboBox ItemsSource="{Binding Languages}" DisplayMemberPath="Name" SelectedValuePath="Code"
                  SelectedValue="{Binding TempSettings.Language}" Margin="0,5,0,15"/>

        <!-- Default Printer -->
        <TextBlock Text="{x:Static resx:Strings.Printer}"/>
        <ComboBox ItemsSource="{Binding Printers}" DisplayMemberPath="Name" SelectedValuePath="Name"
                  SelectedValue="{Binding TempSettings.DefaultPrinter}" Margin="0,5,0,15"/>

        <!-- Default Copies -->
        <TextBlock Text="{x:Static resx:Strings.Copies}"/>
        <TextBox Text="{Binding TempSettings.DefaultCopies}" Margin="0,5,0,15"/>

        <!-- Default Color -->
        <CheckBox IsChecked="{Binding TempSettings.DefaultColor}" Content="{x:Static resx:Strings.Color}" Margin="0,5,0,15"/>

        <!-- UI Scale -->
        <TextBlock Text="UI Scale"/>
        <ComboBox ItemsSource="{Binding UiScales}" SelectedItem="{Binding TempSettings.UiScale}" Margin="0,5,0,15"/>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" Content="{x:Static resx:Strings.Apply}" Width="80" Margin="0,0,10,0" IsDefault="True"/>
            <Button Command="{Binding CancelCommand}" Content="{x:Static resx:Strings.Cancel}" Width="80" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
